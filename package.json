{
  "name": "cookie-test",
  "private": true,
  "scripts": {
    "dev": "bun run ./dev.ts",
    "test": "bun run ./run-test.ts"
  },
  "devDependencies": {
    "agent-browser": "^0.23.0",
    "@types/bun": "^1.3.11"
  }
}
